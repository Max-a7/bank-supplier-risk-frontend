# 前端交付与验收报告（联调完成版）

**项目名称**：基于多智能体协同的银行外包供应商动态风险预测与决策系统

**交付模块**：前端可视化与交互界面

**交付人**：马欣

**交付日期**：2026-09-20

**前端仓库**：https://github.com/Max-a7/bank-supplier-risk-frontend

**联调状态**：✅ 前后端 100% 联调完成，5 个页面全部跑通真实数据

---

## 一、交付内容概述

前端已完成 **5 个核心页面** 的开发，并已与后端（梁雨珊 FastAPI 服务）完成**端到端联调**。

- **数据源**：后端数据库（202 家供应商），通过 `/backend/dashboard/summary` 和 `/backend/suppliers` 接口实时获取。
- **风险分布**：RED 12 家 / YELLOW 15 家 / GREEN 175 家。
- **统一 API 出口**：`src/api/risk.js`
- **字段映射**：`src/utils/mapping.js`

所有页面已通过本地测试，无控制台报错，UI 交互流畅，符合银行金融级系统的视觉规范。

---

## 二、页面验收清单

请团队成员按以下步骤在本地运行前端项目并逐一验收：

### 📋 本地运行方式

```bash
# 1. 拉取前端代码
git clone https://github.com/Max-a7/bank-supplier-risk-frontend.git
cd bank-supplier-risk-frontend

# 2. 安装依赖
npm install

# 3. 启动项目
npm run dev
```

**⚠️ 必须先启动后端**：
```bash
cd C:\Users\maxin\jiujiang0912
.\.venv\Scripts\activate.bat
python -m app.main serve
```

**浏览器访问**：http://localhost:5173/dashboard

---

### ✅ 页面 1：风险驾驶舱（Dashboard）

- **路由**：`/dashboard`
- **数据来源**：`GET /backend/dashboard/summary`
- **验收看点**：
  - 统计卡片：供应商总数 **202**、高风险 **12**、中风险 **15**、待审核 **242**
  - 重点监测名单风险分折线图：X 轴为供应商名称，Y 轴为风险分数（7.56、2.856、2.52...）
  - 风险等级分布饼图：红色 12 + 黄色 15 + 绿色 175
  - Top 5 风险供应商表格：按风险分降序展示
  - 覆盖度标签：`覆盖 202 / 202 家`

---

### ✅ 页面 2：供应商风险列表（SupplierList）

- **路由**：`/suppliers`
- **数据来源**：`GET /backend/suppliers`
- **验收看点**：
  - 表格展示 202 家供应商，包含 ID、名称、风险等级（红/黄/绿标签）、风险分、重要性
  - 支持按供应商名称搜索
  - 支持按风险等级筛选（RED / YELLOW / GREEN）
  - 点击"查看详情"可跳转至 360° 画像页

---

### ✅ 页面 3：供应商 360° 画像（SupplierDetail）

- **路由**：`/suppliers/:id`（例：`/suppliers/DEMO-IMPORTANT`）
- **数据来源**：`GET /backend/suppliers/{id}/reports` + `GET /backend/reports/{report_id}`
- **验收看点**：
  - 顶部时间轴：点击"▶ 播放风险演变"，动态展示风险事件按时序演变
  - 供应商基本信息：名称、ID、合作状态、风险等级
  - **六维度雷达图**：根据 `dimension_breakdown` 动态展示六个维度分数（司法 3.9、经营风险 2.4 等）
  - 证据链时间轴：展示具体风险事件（如"第 35 周｜经营风险｜行政处罚"）
  - 关联项目 + 关系图：展示供应商的项目信息与关系网络

---

### ✅ 页面 4：Agent 研判过程（AgentTrace）

- **路由**：`/agent-trace`
- **数据来源**：`GET /agent/report/{id}`
- **验收看点**：
  - 顶部步骤条：展示 6 个 Agent 节点（维度归类 / 风险识别 / 关联分析 / 证据 / 决策 / 一致性校验）
  - 时间线日志：每个 Agent 的状态（SUCCESS / FALLBACK / DISABLED / SKIPPED）与推理结果
  - 证据链：动态展示关联的 `evidence_id`，可溯源

---

### ✅ 页面 5：风险处置中心（DisposalCenter）

- **路由**：`/disposal`
- **数据来源**：`GET /backend/dashboard/summary`（watchlist）+ `GET /backend/suppliers`
- **验收看点**：
  - 待处置标签页：展示 **12 家 RED 供应商**，按风险分降序排列
  - 表格包含：供应商、风险等级、风险分、处置建议、状态、整改期限、复核人
  - 操作列："确认"与"调整"按钮并排对齐
  - 数据示例：演示重要供应商（7.56）、NOR服务商062（2.856）、NOR服务商014（2.52）...

---

## 三、技术架构说明

| 层级 | 技术选型 | 说明 |
| :--- | :--- | :--- |
| 前端框架 | Vue 3 + Vite | 组件化开发，热更新快速 |
| UI 组件库 | Element Plus | 表格、卡片、步骤条、时间线等 |
| 图表库 | ECharts 5 | 折线图、饼图、雷达图 |
| 路由 | Vue Router | 5 个页面 + 侧边栏菜单 |
| HTTP 客户端 | Axios | 请求拦截器携带 `X-User-Id`，响应拦截器解包信封 |
| 数据层 | `src/api/risk.js` | 统一 API 出口，对接后端 FastAPI |
| 字段映射 | `src/utils/mapping.js` | 风险等级/趋势/处置类型的中文映射 |

---

## 四、前后端联调关键接口

| 功能 | 接口 | 说明 |
| :--- | :--- | :--- |
| Mock 登录 | `POST /backend/mock/login` | 获取 `X-User-Id`（demo-leadership） |
| Dashboard 统计 | `GET /backend/dashboard/summary` | 返回风险分布 + 重点监测名单 |
| 供应商列表 | `GET /backend/suppliers` | 返回全部供应商 |
| 供应商报告列表 | `GET /backend/suppliers/{id}/reports` | 返回该供应商的报告列表 |
| 报告详情 | `GET /backend/reports/{report_id}` | 返回完整报告（含 `dimension_breakdown`、`evidence_summary`） |
| 数据刷新 | `POST /backend/monitoring/refresh-risk-state` | 全量分析并回写数据库风险等级 |

**⚠️ 关键设计**：
1. **ID 归一化**：用正则 `/[\u2010-\u2015\u2212]/g` 兼容特殊连字符 `‑`（U+2011）。
2. **数据解包**：`/backend/reports/{report_id}` 返回的报告内容在 `data.report` 里，需解包。
3. **数据融合**：报告列表有 `risk_level`，完整报告有 `dimension_breakdown`，两者合并。

---

## 五、联调成果

| 指标 | 值 |
| :--- | :--- |
| 供应商总数 | **202** |
| 高风险（RED） | **12** |
| 中风险（YELLOW） | **15** |
| 低风险（GREEN） | **175** |
| 待审核数 | **242** |
| 覆盖度 | **202 / 202** |

---

## 六、待办事项与协作请求

| 事项 | 负责人 | 说明 |
| :--- | :--- | :--- |
| Agent 真实输出对接 | 李芳痕 | 等 Agent 模块输出真实 `llm_node_status` 后替换 |
| 关联项目/关系图接口 | 梁雨珊 | 目前前端为静态数据，后端可提供真实接口 |
| 答辩 PPT 准备 | 马欣 | 已准备 5 张联调成功截图 |

---

## 七、截图预览

见 `docs/screenshots/` 目录：
1. `01-dashboard.png` — 风险驾驶舱
2. `02-supplier-list.png` — 供应商风险列表
3. `03-supplier-detail.png` — 供应商 360° 画像
4. `04-agent-trace.png` — Agent 研判过程
5. `05-disposal-center.png` — 风险处置中心

---

## 八、总结

前端所有页面已全部跑通，并完成与后端的端到端联调。当前已具备以下能力：

- ✅ 5 个核心业务页面完整覆盖
- ✅ 202 家供应商真实数据动态渲染
- ✅ 六维度雷达图、时序时间轴、证据链溯源、多 Agent 状态可视化
- ✅ 与后端接口契约完全对齐
- ✅ Mock 登录 + `X-User-Id` 鉴权链路完整
- ✅ 数据刷新接口（`refresh-risk-state`）解决数据库风险等级同步问题

**前端交付完成，可以进入答辩准备阶段。**

---

## 📌 下一步：把这份报告推送到 GitHub

```bash
cd C:\Users\maxin\bank-supplier-risk-frontend
git add .
git commit -m "docs: 更新前端交付报告为联调完成版"
git push
```
---